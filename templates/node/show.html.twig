{% extends 'base.html.twig' %}

{% block stylesheets %}
<style>
article{
  img{
    max-width: 100%;
    object-fit: contain;
    max-height: 400px;
    height: auto;
  }
}
</style>
{% endblock %}

{% block main %}
<main class="main">
    {{ include('block/hero.html.twig') }}

    <div class="container node-body">
      <img class="w-100 mt-5" src="{{asset("images/#{node.image}")}}">
      <h3 class="node-title py-5 text-center">{{ node.title }}</h3>
      <article class="my-5">
        {{ node.body|raw }}
      </article>
    </div>

  </main>
{% endblock %}

{% block javascript %}
{% endblock %}
